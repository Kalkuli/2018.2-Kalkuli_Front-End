version: '3.6'

services:
  front-end:
    build:
      context: .
      dockerfile: Dockerfile-dev
    ports:
      - 3000:3000  
    volumes:
      - '.:/app'
      - '/app/node_modules'
    environment:
      - NODE_ENV=development